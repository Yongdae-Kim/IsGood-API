<%= form_for(common_code) do |f| %>
  <% if common_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(common_code.errors.count, "error") %> prohibited this common_code from being saved:</h2>

      <ul>
      <% common_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :main_id %>
    <%= f.text_field :main_id %>
  </div>

  <div class="field">
    <%= f.label :detail_id %>
    <%= f.text_field :detail_id %>
  </div>

  <div class="field">
    <%= f.label :code_name %>
    <%= f.text_field :code_name %>
  </div>

  <div class="field">
    <%= f.label :sort_set %>
    <%= f.text_field :sort_set %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
